# =============================================
# Pool-Printer – Umgebungsvariablen
# =============================================
# Kopiere diese Datei nach .env.local und passe die Werte an:
#   cp .env.example .env.local
# =============================================

# Geheimer Schlüssel für JWT-Token-Verschlüsselung (NextAuth)
# Generieren mit: openssl rand -base64 32
NEXTAUTH_SECRET=

# Basis-URL der Web-App (auch von der Print Middleware verwendet)
NEXTAUTH_URL=http://localhost:3000

# API-Schlüssel für die Kommunikation zwischen Print Middleware und Web-App
# Muss in beiden Konfigurationen identisch sein
API_KEY=
